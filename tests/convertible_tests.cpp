#include <convertible/convertible.hpp>

#include <catch2/catch.hpp>

SCENARIO("convertible_tests")
{
    GIVEN("this")
    {
        THEN("that")
        {
            REQUIRE(false);
        }
    }
}